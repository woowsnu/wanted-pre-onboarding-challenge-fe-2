// @ts-check

/**
 * Todo 추가 (단, content는 필수)
 * @param {string} id 
 * @param {string} content 
 * @param {number} category 
 * @returns 
 */
function createTodo(id, content, category) {
  return;
}

/**
 * Todo 조회 (Id로 특정 Todo 조회도 가능)
 * @param {string} id 
 * @returns 
 */
function readTodo(id) {
  return;
}

/**
 * Todo 수정 (Id는 수정 불가)
 * @param {string} id 
 * @param {string} content 
 * @param {boolean} isCompleted 
 * @param {number} category 
 * @param {string} tags 
 * @returns 
 */
function updateTodo(id, content, isCompleted, category, tags) {
  return;
}

/**
 * Todo 삭제 
 * @param {string} id 
 * @param {string} tags 
 * @returns 
 */
function deleteTodo(id, tags) {
  return;
}
